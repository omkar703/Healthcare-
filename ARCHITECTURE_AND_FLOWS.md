# Medical AI System - Architecture & Data Flow

## 🏗️ System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          FRONTEND (React/Next.js)                        │
│                    Patient Portal  |  Doctor Portal                      │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             │ HTTP/REST API
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      MEDICAL AI MICROSERVICE                             │
│                         (FastAPI - Port 8000)                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                      API LAYER (FastAPI)                         │   │
│  ├─────────────────────────────────────────────────────────────────┤   │
│  │  • /api/v1/chat/*           - Patient & Doctor Chat             │   │
│  │  • /api/v1/breast-cancer/*  - Risk Assessment                   │   │
│  │  • /api/v1/patients/*       - Patient Management                │   │
│  │  • /api/v1/doctors/*        - Doctor Management                 │   │
│  │  • /api/v1/ocr/*            - Credential Extraction             │   │
│  │  • /api/v1/rag/*            - RAG Service                       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    BUSINESS LOGIC LAYER                          │   │
│  ├─────────────────────────────────────────────────────────────────┤   │
│  │  ┌──────────────────┐  ┌──────────────────┐  ┌───────────────┐ │   │
│  │  │ AI Guardrails    │  │ Chat Service     │  │ RAG Service   │ │   │
│  │  │ Service          │  │                  │  │               │ │   │
│  │  │ • Emergency      │  │ • Orchestration  │  │ • Vector      │ │   │
│  │  │   Detection      │  │ • Context        │  │   Search      │ │   │
│  │  │ • Complex Query  │  │   Building       │  │ • Chunk       │ │   │
│  │  │   Filter         │  │ • Response       │  │   Retrieval   │ │   │
│  │  │ • Simplification │  │   Generation     │  │               │ │   │
│  │  └──────────────────┘  └──────────────────┘  └───────────────┘ │   │
│  │                                                                  │   │
│  │  ┌──────────────────┐  ┌──────────────────┐  ┌───────────────┐ │   │
│  │  │ Document         │  │ Health Scoring   │  │ Risk          │ │   │
│  │  │ Processor        │  │ Service          │  │ Assessment    │ │   │
│  │  │ • 3-Tier         │  │ • Component      │  │ • Multi-      │ │   │
│  │  │   Pipeline       │  │   Scoring        │  │   Category    │ │   │
│  │  │ • OCR            │  │ • Trend          │  │ • Markers     │ │   │
│  │  │ • Vision         │  │   Analysis       │  │ • Recommend   │ │   │
│  │  │ • Indexing       │  │                  │  │               │ │   │
│  │  └──────────────────┘  └──────────────────┘  └───────────────┘ │   │
│  │                                                                  │   │
│  │  ┌──────────────────────────────────────────────────────────┐  │   │
│  │  │ Breast Cancer Scoring Service                             │  │   │
│  │  │ • Risk Calculation (0-100)                                │  │   │
│  │  │ • Critical Flag Detection                                 │  │   │
│  │  │ • Lab Test Stage Determination                            │  │   │
│  │  └──────────────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    EXTERNAL SERVICES                             │   │
│  ├─────────────────────────────────────────────────────────────────┤   │
│  │  ┌──────────────────┐  ┌──────────────────┐  ┌───────────────┐ │   │
│  │  │ AWS Bedrock      │  │ AWS Textract     │  │ AWS Titan     │ │   │
│  │  │ (Claude 3.5)     │  │ (OCR)            │  │ (Embeddings)  │ │   │
│  │  │ • Text Gen       │  │ • PDF Extract    │  │ • Vector Gen  │ │   │
│  │  │ • Vision         │  │ • Table Recog    │  │               │ │   │
│  │  └──────────────────┘  └──────────────────┘  └───────────────┘ │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        DATA LAYER                                        │
├─────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────┐  ┌────────────────────────────────┐   │
│  │ PostgreSQL + pgvector       │  │ Redis                          │   │
│  │ (Port 5433)                 │  │ (Port 6379)                    │   │
│  ├─────────────────────────────┤  ├────────────────────────────────┤   │
│  │ • patients                  │  │ • Celery Task Queue            │   │
│  │ • doctors                   │  │ • Session Cache                │   │
│  │ • medical_documents         │  │                                │   │
│  │ • health_scores             │  │                                │   │
│  │ • risk_assessments          │  │                                │   │
│  │ • document_chunks (vector)  │  │                                │   │
│  │ • patient_conversations     │  │                                │   │
│  │ • doctor_conversations      │  │                                │   │
│  └─────────────────────────────┘  └────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    ASYNC PROCESSING LAYER                                │
├─────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────┐  ┌────────────────────────────────┐   │
│  │ Celery Worker               │  │ Celery Beat                    │   │
│  │ • Document Processing       │  │ • Scheduled Tasks              │   │
│  │ • OCR Tasks                 │  │ • Health Score Updates         │   │
│  │ • RAG Indexing              │  │ • Risk Reassessment            │   │
│  └─────────────────────────────┘  └────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagrams

### 1. Patient Chat Flow

```
┌──────────┐
│ Patient  │
│ Frontend │
└────┬─────┘
     │
     │ 1. POST /api/v1/chat/patient/{uuid}
     │    {"message": "What are my test results?"}
     ▼
┌────────────────┐
│  Chat API      │
│  Endpoint      │
└────┬───────────┘
     │
     │ 2. Check AI Guardrails
     ▼
┌────────────────┐
│ AI Guardrails  │──────┐
│ Service        │      │ Emergency? → Return urgent message
└────┬───────────┘      │ Complex?   → Redirect to doctor
     │                  └─────────────────────────────────┐
     │ 3. Safe to proceed                                 │
     ▼                                                     ▼
┌────────────────┐                                  ┌──────────┐
│ RAG Service    │                                  │ Response │
│                │                                  │ to User  │
└────┬───────────┘                                  └──────────┘
     │
     │ 4. Vector similarity search
     │    Get relevant document chunks
     ▼
┌────────────────┐
│ PostgreSQL     │
│ (pgvector)     │
└────┬───────────┘
     │
     │ 5. Return top 5 relevant chunks
     ▼
┌────────────────┐
│ Chat Service   │
└────┬───────────┘
     │
     │ 6. Build context + prompt
     ▼
┌────────────────┐
│ AWS Bedrock    │
│ (Claude 3.5)   │
└────┬───────────┘
     │
     │ 7. Generate AI response
     ▼
┌────────────────┐
│ AI Guardrails  │
│ (Post-process) │
└────┬───────────┘
     │
     │ 8. Simplify terminology
     │    Add disclaimer
     ▼
┌────────────────┐
│ Save to DB     │
│ (conversation) │
└────┬───────────┘
     │
     │ 9. Return response
     ▼
┌──────────┐
│ Patient  │
│ Frontend │
└──────────┘
```

---

### 2. Document Upload & Processing Flow

```
┌──────────┐
│ Patient/ │
│ Doctor   │
└────┬─────┘
     │
     │ 1. POST /upload (multipart/form-data)
     ▼
┌────────────────┐
│ Upload API     │
│ Endpoint       │
└────┬───────────┘
     │
     │ 2. Validate file (type, size)
     │    Save to _documents/
     ▼
┌────────────────┐
│ Create DB      │
│ Record         │
│ (status:       │
│  UPLOADED)     │
└────┬───────────┘
     │
     │ 3. Trigger async processing
     │    Return document_id to user
     ▼
┌────────────────┐
│ Celery Worker  │
└────┬───────────┘
     │
     │ ┌─────────────────────────────────────┐
     │ │   3-TIER PROCESSING PIPELINE        │
     │ └─────────────────────────────────────┘
     │
     │ TIER 1: OCR Extraction
     ▼
┌────────────────┐
│ AWS Textract   │
│ Service        │
└────┬───────────┘
     │
     │ • Extract all text
     │ • Recognize tables/forms
     │ • Store in tier_1_text
     ▼
┌────────────────┐
│ Update DB      │
│ (tier_1_       │
│  complete)     │
└────┬───────────┘
     │
     │ TIER 2: Vision Analysis
     ▼
┌────────────────┐
│ AWS Bedrock    │
│ Vision         │
│ (Claude 3.5)   │
└────┬───────────┘
     │
     │ • Analyze image
     │ • Extract medical insights
     │ • Identify document type
     │ • Detect risk markers
     │ • Store in tier_2_enriched
     ▼
┌────────────────┐
│ Update DB      │
│ (tier_2_       │
│  complete)     │
└────┬───────────┘
     │
     │ TIER 3: RAG Indexing
     ▼
┌────────────────┐
│ Text Chunking  │
│ Service        │
└────┬───────────┘
     │
     │ • Split into semantic chunks
     ▼
┌────────────────┐
│ AWS Titan      │
│ Embeddings     │
└────┬───────────┘
     │
     │ • Generate vector embeddings
     ▼
┌────────────────┐
│ PostgreSQL     │
│ (pgvector)     │
│ document_      │
│ chunks table   │
└────┬───────────┘
     │
     │ • Store chunks with embeddings
     ▼
┌────────────────┐
│ Update DB      │
│ (status:       │
│  COMPLETED)    │
└────┬───────────┘
     │
     │ 4. Document ready for RAG
     ▼
┌──────────┐
│ Frontend │
│ (Poll    │
│  status) │
└──────────┘
```

---

### 3. Breast Cancer Risk Assessment Flow

```
┌──────────┐
│ Patient  │
│ Frontend │
└────┬─────┘
     │
     │ 1. POST /api/v1/breast-cancer/assess
     │    (40-question data)
     ▼
┌────────────────────────────────┐
│ Breast Cancer Assessment API   │
└────┬───────────────────────────┘
     │
     │ 2. Validate request schema
     ▼
┌────────────────────────────────┐
│ Breast Cancer Scoring Service  │
└────┬───────────────────────────┘
     │
     │ ┌──────────────────────────────────┐
     │ │   SCORING ALGORITHM              │
     │ └──────────────────────────────────┘
     │
     │ 3. Start with score = 100
     ▼
┌────────────────────────────────┐
│ Check Critical Flags           │
│ • Hard/fixed lump? -50         │
│ • Bloody discharge? -40        │
│ • BRCA mutation? -45           │
│ • Skin dimpling? -35           │
│ • Nipple retraction? -30       │
└────┬───────────────────────────┘
     │
     │ 4. Apply deductions
     ▼
┌────────────────────────────────┐
│ Calculate Risk Factors         │
│ • New lump: -20                │
│ • Dense breasts: -15           │
│ • Family history: -18          │
│ • Localized pain: -15          │
│ • Long-term OCP: -10           │
│ • etc...                       │
└────┬───────────────────────────┘
     │
     │ 5. Final score (0-100)
     ▼
┌────────────────────────────────┐
│ Determine Risk Level           │
│ • 0-40:   HIGH                 │
│ • 41-75:  MEDIUM               │
│ • 76-100: LOW                  │
└────┬───────────────────────────┘
     │
     │ 6. Assign Lab Test Stage
     ▼
┌────────────────────────────────┐
│ Lab Test Stage Assignment      │
│ • Stage 1: Basic health panel  │
│ • Stage 2: + Genetic + Tumor   │
│ • Stage 3: + Biopsy + Advanced │
└────┬───────────────────────────┘
     │
     │ 7. Generate recommendations
     ▼
┌────────────────────────────────┐
│ Build Response                 │
│ • Score                        │
│ • Risk level                   │
│ • Recommendation text          │
│ • Required lab tests           │
│ • Reasoning                    │
│ • Critical flags               │
└────┬───────────────────────────┘
     │
     │ 8. Save to patient record
     ▼
┌────────────────────────────────┐
│ PostgreSQL                     │
│ (patients.breast_cancer_       │
│  screening JSONB field)        │
└────┬───────────────────────────┘
     │
     │ 9. Return response
     ▼
┌──────────┐
│ Patient  │
│ Frontend │
└──────────┘
```

---

### 4. Doctor Patient Analysis Flow

```
┌──────────┐
│ Doctor   │
│ Frontend │
└────┬─────┘
     │
     │ 1. POST /api/v1/chat/doctor/{doctor_uuid}/patient/{patient_uuid}
     │    {"message": "Analyze tumor markers"}
     ▼
┌────────────────┐
│ Doctor Chat    │
│ API Endpoint   │
└────┬───────────┘
     │
     │ 2. Verify doctor & patient exist
     ▼
┌────────────────┐
│ Get Patient    │
│ Summary        │
│ • Health score │
│ • Risk level   │
│ • Demographics │
└────┬───────────┘
     │
     │ 3. Retrieve comprehensive context
     ▼
┌────────────────┐
│ RAG Service    │
│ (top_k=10)     │
└────┬───────────┘
     │
     │ 4. Vector search for relevant docs
     ▼
┌────────────────┐
│ PostgreSQL     │
│ (pgvector)     │
└────┬───────────┘
     │
     │ 5. Return top 10 chunks
     ▼
┌────────────────┐
│ Chat Service   │
└────┬───────────┘
     │
     │ 6. Build detailed medical context
     │    NO guardrails applied
     ▼
┌────────────────┐
│ AWS Bedrock    │
│ (Claude 3.5)   │
└────┬───────────┘
     │
     │ 7. Generate detailed medical analysis
     │    Full medical terminology
     ▼
┌────────────────┐
│ Save to DB     │
│ (doctor_       │
│  conversation) │
└────┬───────────┘
     │
     │ 8. Return response with:
     │    • AI analysis
     │    • Source documents
     │    • Patient summary
     ▼
┌──────────┐
│ Doctor   │
│ Frontend │
└──────────┘
```

---

## 🔄 Key Processing Pipelines

### Health Score Calculation Pipeline
```
Patient Data → Component Scoring → Weighted Average → Trend Analysis → Final Score
     ↓              ↓                    ↓                  ↓              ↓
Questionnaire   Cardiovascular      Apply weights      Compare to      Store in DB
+ Lab Data      Metabolic (0.25)    (0.25, 0.2,       previous        + Generate
+ Documents     Respiratory (0.2)    0.15, etc.)       scores          recommendations
                Mental (0.15)
                Lifestyle (0.15)
                Preventive (0.15)
                            (0.1)
```

### Risk Assessment Pipeline
```
Patient Data → Multi-Category Analysis → Risk Scoring → Recommendations
     ↓              ↓                         ↓              ↓
Medical        Cardiovascular Risk       Aggregate      Personalized
History        Diabetes Risk             scores         action items
Symptoms       Cancer Risk               ↓              ↓
Lab Results    Other Risks           Categorize     Schedule tests
Family Hx                            (High/Med/Low)  Lifestyle changes
```

### Document Processing Timeline
```
Upload (0s) → Tier 1 OCR (5-15s) → Tier 2 Vision (10-20s) → Tier 3 RAG (5-10s) → Complete (30-45s)
     ↓              ↓                      ↓                      ↓                    ↓
Save file      Extract text          Analyze image         Generate embeddings    Ready for
Create DB      Recognize tables      Identify type         Store vectors          AI chat
record         Store raw text        Extract insights      Index chunks           queries
```

---

## 🎯 Component Interaction Matrix

| Component | Interacts With | Purpose |
|-----------|----------------|---------|
| **Chat API** | AI Guardrails, RAG Service, Chat Service, AWS Bedrock | Orchestrate chat flow |
| **AI Guardrails** | Chat Service, AWS Bedrock | Patient safety filtering |
| **RAG Service** | PostgreSQL (pgvector), AWS Titan | Context retrieval |
| **Document Processor** | AWS Textract, AWS Bedrock, RAG Service, Celery | 3-tier processing |
| **Breast Cancer Scoring** | PostgreSQL | Risk calculation |
| **Health Scoring** | PostgreSQL | Health score calculation |
| **Chat Service** | AWS Bedrock, RAG Service, Guardrails | Response generation |
| **Celery Worker** | Redis, PostgreSQL, All Services | Async task execution |

---

## 📦 Data Storage Strategy

### PostgreSQL Tables
```
patients
├── patient_uuid (PK)
├── demographic_data (JSONB)
├── questionnaire_responses (JSONB)
└── breast_cancer_screening (JSONB)

medical_documents
├── document_id (PK)
├── patient_uuid (FK)
├── tier_1_text (TEXT)
├── tier_2_enriched (JSONB)
├── tier_3_indexed (BOOLEAN)
└── processing_status (ENUM)

document_chunks
├── chunk_id (PK)
├── document_id (FK)
├── patient_uuid (FK)
├── chunk_text (TEXT)
├── embedding (VECTOR)  ← pgvector
└── metadata (JSONB)

patient_conversations
├── conversation_id (PK)
├── patient_uuid (FK)
└── messages (JSONB[])

doctor_conversations
├── conversation_id (PK)
├── doctor_uuid (FK)
├── patient_uuid (FK)
└── messages (JSONB[])
```

---

**Last Updated**: 2026-02-09
**Version**: 1.0.0
